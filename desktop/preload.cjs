// DOM marker only; no Node or IPC API is exposed to the renderer.
if(process.platform==='darwin')window.addEventListener('DOMContentLoaded',()=>document.documentElement.classList.add('wj-desktop-macos'),{once:true});
