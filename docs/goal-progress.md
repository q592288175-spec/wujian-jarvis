# JARVIS 完成审计（持续更新）

2026-09-19。目标保持完整，不以已有功能替代待办。

|要求|当前证据|状态|
|---|---|---|
|真实行情覆盖、流动性、昨收口径|88订阅，快照筛后61；服务端重算昨收；JS与Python边界测试|已验证当前快照，非所有月份|
|报价时间与收盘证据|上海时区时效标记；Quote.close与last分列；11/61返回close；日K末根仍按官方规则未完成|已接字段，覆盖缺口显式保留|
|研究报告来源、反证与情景|实际DeepSeek IM报告、9条工具记录、2份正文、报告结构检查|已验证流程；事实仍待人工核验|
|保存和历史判断跟踪|上一报告关联、换月边界、用户复核侧文件；页面TEST.UI保存回读成功并清理|已验证，未生成概率后验|
|任务稳定性|启动锁、取消、原子写、查询退避；39项JS和7项Python测试|部分完成；中断需手动重试|
|备份回滚|本机源码压缩包与回滚说明|已有；桌面版本升级恢复尚待演练|
|GPT-Live全双工与DeepSeek后台|仅检查到DeepSeek配置，OPENAI_API_KEY不存在|未完成，已向用户说明本地配置方式|
|桌面应用|Electron主进程、启动管理、隔离权限、构建脚本；官方二进制断点下载中|未生成/未原生验收，不宣称完成|

下一检查点：校验Electron包并构建，冷启动验证服务与行情，检查独立窗口研究页和后台任务，然后继续缺口项。

2026-09-19 启动恢复复核：确认没有运行中的研究后重启 Web 服务，`/api/health` 返回工作台标识与 0.3.0 版本；历史报告列表仍能读取（7 条，含取消/失败记录，不代表 7 份成功报告）。桌面启动的行情状态读取增加 5 秒超时，避免启动页无限等待。Electron 下载进程仍在运行，约 36MB；应用构建与原生验收仍未完成。

2026-09-19 完整源码恢复演练通过：旧 before-research-loop.tar.gz 缺少 finance.mjs 与 data/market.json，只能覆盖部分源码。新增 scripts/checkpoint.py，检查点 20260919-112247-source.tar.gz 在隔离目录、4329端口实际启动，健康接口、首页、空报告列表均通过；没有复制凭据，临时服务已关闭。桌面应用升级恢复仍待验收。

2026-09-19 研究简报补充独立 closeBreadth，收盘统计仅计有有效收盘字段的样本，最新成交统计显式标 last/preClose 口径；不以最新价或结算价补齐。包含盘中涨、收盘跌及缺失收盘样本的8项研究测试通过。Electron上一下载进程明确以18退出，已从已有文件断点续传，未重头覆盖。

2026-09-19 运行接口验证：服务重启后研究简报返回 v1.3，当前61个有效研究池合约中11个具备收盘字段（8涨2跌1平，仅该样本）。另修正coverage把筛选后数量误称订阅数量的问题：源码改为subscribed原订阅数量、retained筛后数量、valid有效报价数量，9项研究测试通过；此最后字段调整待下次服务重启加载。

2026-09-19 集成回归：最新41项JavaScript测试全部通过，覆盖昨收/收盘口径、量仓筛选、报告保存与复核、任务取消和语音停止播报的边界。该测试结果不替代麦克风通话或Electron原生验收。下载进程15247仍运行，文件约50MB，官方校验值已可用。

2026-09-19 状态语义修正：发现 /api/status 把 DeepSeek 密钥存在误报为 realtimeConfigured。已拆分 chatConfigured 与 realtimeConfigured（当前为 false），首页按文字模型配置判断可用性，避免将浏览器语音误称全双工。语法检查通过，待服务重启后加载后端字段；真正实时语音尚未接通。

2026-09-19 最新服务加载验证通过：确认无运行研究后重启，/api/status 返回 chatConfigured=true、realtimeConfigured=false；研究简报 coverage 返回订阅88、筛后61、有效61。之前待重启的字段调整现已在运行接口生效。下载15247仍处于运行状态。

2026-09-19 桌面构建完成：官方Electron文件下载退出0，构建脚本SHA256校验通过，成功生成 .runtime/desktop-build/五简 JARVIS-darwin-arm64/五简 JARVIS.app。通过原生应用控制实际打开，截图显示稳定版首页、真实行情池61个、底栏和研究入口；本次复用现有服务。尚待验证停止外部服务后的冷启动、退出清理、研究页操作以及真实语音，不将打开首页当成全部完成。

2026-09-19 原生研究页面验收：在五简JARVIS.app点击“深度研究”，成功展示品种输入、最近研究；打开既有中证1000报告，实际显示报价/交易日/采集时间、来源审计、上一版入口、Markdown下载和复核记录表单。没有发起新模型调用或写入测试判断。本次证明原生窗口可读取已有报告，不代表全文金融事实核验或真实语音验收。

2026-09-19 桌面服务生命周期验收：无运行研究时退出应用，停止原Web服务并确认4318不可访问；重新打开.app后健康接口恢复且窗口加载工作台，证明可自行启动Web服务。菜单退出后4318再次不可访问，证明自有Web子进程被清理；随后重新打开恢复使用。原有独立行情采集器本轮未停止，因此行情采集器冷启动与退出清理尚未验证。

2026-09-19 行情生命周期验收通过：停止原独立采集器后由.app启动新Python采集器，接口从connecting转observing并返回61个合约；退出应用后Web端口不可访问，采集包装进程与Python进程均消失。修正快速重开可能因旧心跳新鲜而漏启采集器的问题：每次启动尝试拉起，由Python独占文件锁防止重复采集。已重新构建并打开；快速重开回归待下一步完成。

2026-09-19 快速重开回归通过：退出后立即重新打开.app，启动了新的独立采集器，接口从connecting恢复observing，61个合约恢复；没有被新鲜但已失效的旧心跳阻止。当前桌面应用保持打开。

2026-09-19 桌面回滚产物：.runtime/checkpoints/desktop-0.3.0-verified.zip 已创建并解压到独立恢复目录；271个文件/链接的SHA256、链接目标和权限与已验证应用完全一致。未覆盖运行应用。该包仅恢复桌面壳；源码、Python环境与本地研究记录仍独立，不构成全部数据灾备。

## 2026-09-19 真实数据统一与GPT-Live接口

用户确认范围仅国内期货，盘立方公司及外接权限未知。清除首页/雷达/决策中的生成行情和模拟审批；真实选品种统一到证据和研究。实际日K扩到1000请求，周/月聚合、MA10与区间测量冻结进入新报告。51项JS测试、7项Python测试通过；稳定/预览网页切换与沪铜研究表单联动通过，浏览器未报错。GPT-Live官方client delegation到DeepSeek已实现，未配置OpenAI key，真实全双工验收仍未完成。详情见real-data-and-live.md。
